import React from 'react';
import s from './Footer.module.css'

const Footer = (props) => {
    return (
        <div className='footer'>
            Footer
        </div>
    );
};

export default Footer;