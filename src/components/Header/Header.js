import React from 'react';
import s from './Header.module.css'

const Header = (props) => {
    return (
        <div className='header'>
            Header
        </div>
    );
};

export default Header;