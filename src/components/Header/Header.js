import React from 'react';
import s from './Header.module.css'

const Header = (props) => {
    return (
        <div className='header'>
            HeaderTESTT PC
        </div>
    );
};

export default Header;